name: babble
channels:
  - conda-forge
dependencies:
  - python=3.6
  - jupyter           # Used by tutorials/ only
  - nb_conda_kernels  # Used by tutorials/ only
  - nltk              # Used by metal/contrib/
  - nose              # Used by tests/ only
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - spacy>=2
  - pip:
    - snorkel-metal==0.1.5